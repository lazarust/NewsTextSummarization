version: "3.8"

services:
  newssite:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./newssite:/newssite
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"